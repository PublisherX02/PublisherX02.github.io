services:
  - type: web
    name: backstop-safe-demo
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn web_demo:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: ALPACA_PAPER_TRADE
        value: "false"
