# Adversarial Conformance Audit — mcp-trade-firewall (Round 4)

Audit date: 2026-08-28. Scope: `C:\Users\moham\Trading Agent`, branch `dev`,
HEAD `87a03f4` plus an uncommitted working-tree diff (the round-4 remediation
under audit: corpus grammar-header consolidation into `evals/GRAMMAR.md`,
`evals/predicates.py` making `forwarded=` mandatory, a regenerated
`evals/out/results.json`, and the eight new option/hedge rule files already
present as untracked files: `hedge_cost_cap`, `hedge_proposal`,
`hedge_regime_call_guard`, `iv_hv_ratio`, `net_delta_floor`,
`option_expiry_floor`, `option_sell_guard`, `option_spread_guard`).

**AUDIT-ONLY discipline**: no file under `src/` was edited during this pass.
All rule-breaking mutation testing (D3) was done via in-memory
`exec()`/`sys.modules` substitution in disposable subprocesses, reading the
real file's text and mutating a string in memory only — never writing to
disk. `git status --short` at the end of this pass matches the same set of
modified/untracked paths as at the start (see the session's opening
`git status` for the exact list); nothing in `src/` changed.

**Independence discipline**: the repository already contains a large,
previous `AUDIT.md` (970 lines, written in the same narrative style as this
one, by whatever process built the features it describes). Per this round's
instructions, that document — and every test file alongside the code it
tests — was treated as an unverified claim, not evidence. Every row below
was independently re-derived by executing code in this session: running
`pytest`, constructing `PolicyEngine`/`build_proxy()` instances directly,
mocking only the HTTP transport layer (never higher-level fetchers) where a
"real" code path was required, and mutating rule logic to confirm tests are
load-bearing. Where the prior document's conclusion held up under
re-execution, that is stated. Where it did not — or where it was itself
untested narrative — that is stated too.

**Headline finding for this round**: the round-3 remediation genuinely
closed several real defects (B1/B3/B4's predicate-grammar looseness is now
enforced by a strict parser that raises on omission, re-verified live) and
the five newly-required live checks all came back clean — the options
corridor has real teeth outside `compute_proposal()`, option sells are
hard-blocked in both single- and multi-leg shapes, `compute_proposal()`
provably cannot reach upstream, and the two README options disclaimers are
substantively accurate. But this pass also surfaced a genuine, previously
undocumented defect: the drawdown-triggered hedge proposal's "which position
to flag" logic (`_largest_open_position_from_history` in
`src/firewall/rules/hedge_proposal.py`) silently drops any order event whose
`price` is `None` — which is exactly what a plain **market** order produces,
the schema-documented default and by far the most common real order shape.
Demonstrated live: seeding a position via a realistic market order (no
`limit_price`) and then breaching the drawdown threshold produces **zero**
hedge-proposal audit records, where the identical scenario with a limit
order produces one. Every existing test of this path (including this
session's own reproduction attempts before this was traced) supplies
`limit_price` on every call, so this was untested, not merely under-tested.
Also unresolved from prior rounds: bracket/OCO/multi-leg orders remain fully
unguarded by size/risk rules at the leg level (demonstrated live below), and
`replace_order_by_id`/symbol-dependent rules still don't cooperate.

---

## A. Interposition completeness

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| A1 | **PASS** | Live enumeration via `uvx alpaca-mcp-server` (network-available this session) + `fastmcp.Client.list_tools()` against the real upstream: **72 tools**. Same enumeration through `build_proxy()` (real backend, real `default.yaml`): **72 tools, identical names**. `set(upstream) - set(proxy)` and `set(proxy) - set(upstream)` are both empty. Ran `PolicyEngine.from_yaml("policies/default.yaml").evaluate(name, {}, {})` against all 72 real tool names: every one gets a definitive decision — 59 `allow`, 13 `hard_block` (10 via `unrecognized-tool-catchall`: `add_asset_to_watchlist_by_id`, `close_position`, `create_locate`, `create_watchlist`, `delete_watchlist_by_id`, `do_not_exercise_options_position`, `exercise_options_position`, `remove_asset_from_watchlist_by_id`, `update_account_config`, `update_watchlist_by_id`; 3 via other rules on an empty-args call). **0/72 bypass the engine.** (Tool count is 72, not the 74 a prior round recorded — the upstream package's pinned version moved; re-enumerated live rather than reusing the old number.) | None. |
| A2 | **PASS** | Re-ran the exact live simulation: a fake `FastMCP` upstream exposing `totally_novel_admin_tool_xyz` (registered so it genuinely appears in `tools/list`), through `build_proxy(backend=fake_upstream, policy_engine=<real default.yaml engine>)`, called with `{"amount": "999999999"}`. Confirmed the tool **is** re-exposed via `tools/list` (`True`), but calling it returns `is_error=True`, message `BLOCKED by rule 'unrecognized-tool-catchall': unrecognized tool 'totally_novel_admin_tool_xyz' — not covered by any policy rule, failing closed`, and — checked directly, not inferred — the fake tool's own handler body **never executed** (`novel_tool_ran["called"] == False`). Unknown-tool-passes-through would be a FAIL; this is not that. | None for the A2 scenario itself. `unrecognized_tool_catchall`'s `covered_patterns`/`read_only_whitelist` remain hand-maintained duplicates of every other rule's `tool_match` (not introspected at runtime — confirmed by reading `src/firewall/rules/unrecognized_tool_catchall.py`'s own docstring), a manual-sync footgun with no drift test. |
| A3 | **PASS** | `grep -rn "call_next\|create_proxy\|Client(" src/` finds all three only in `src/firewall/proxy.py`: `call_next` inside `FirewallMiddleware.on_call_tool`; `Client(` inside `_alpaca_client`; `create_proxy` inside `build_proxy`. No other file references any of them. Confirmed the one call site is downstream of `policy_engine.evaluate(...)` by reading `on_call_tool` directly (`proxy.py:503-581`): `evaluate()` runs, then (for allow/soft_block only) `record_call_pending` → `call_next(context)` → `record_call_outcome`; a `hard_block` verdict raises `ToolError` before `call_next` is ever reached — confirmed live via A2 above (handler body never ran). **Improvement over a prior round's caveat**: `FirewallMiddleware` now also overrides `on_read_resource`/`on_get_prompt` (`proxy.py:583-620`), each unconditionally raising `ResourceError`/`PromptError` and logging a `hard_block` audit record — the "0 resources/prompts exposed today, but structurally only one of three middleware hooks is covered" gap a prior round flagged as latent is now closed by code, not just by the current upstream's shape. `src/firewall/market_data.py`/`account_data.py` make their own direct HTTP calls to Alpaca's data/account APIs for rules' internal risk math — a distinct network path, not reachable by or bypassable via an MCP client's tool call, so it does not weaken this finding. | None. |
| A4 | **FAIL** | Executed three aliasing shapes live against the real `PolicyEngine`/`default.yaml` (cvar-gate/pct-of-adv disabled only to isolate the sizing rules' own behavior from an unrelated missing-`account_equity` block): **(1) STOCK bracket order** — parent `qty=10, limit_price=$50` ($500, under caps) with `take_profit={"limit_price":"9999999.00"}`/`stop_loss={"stop_price":"0.01"}` → `verdict=allow`. No rule reads `take_profit`/`stop_loss` at all; the bracket's real risk is entirely unguarded. **(2) OCO order** (`order_class="oco"`) — same result, `allow`. **(3) Multi-leg option order with no parent `limit_price`** (a legitimate net-debit market order on two far-dated, allowlisted, all-buy legs) → `allow`, confirmed via a spy that `cvar-gate`'s bars fetcher was never even called: `notional_cap` (the only sizing rule that reads `legs` at all for options) computes `None` notional from the missing parent price and no-ops, exactly as it does for a price-less single-leg market order — meaning a multi-leg options order can be submitted with **zero** notional/position/CVaR/ADV checks of any kind. **(4) `replace_order_by_id`** bumping `qty` only (a real, Alpaca-legal partial amendment) correctly fails closed (`hard_block`, `notional-cap-single-order`, "cannot compute notional — incomplete order data"). **(5) `blast_radius` corridor** — `cancel_all_orders`/`close_all_positions`/`liquidate_all` all correctly hard-block via `blast-radius-bulk-actions` by default (`human_approved` unset). Rules match on tool NAME + a `symbol`/`qty`/`limit_price` field precondition, not on order-action semantics — a bracket/OCO/multi-leg-no-price order performs a real order action but is invisible to every size/risk rule except the accidentally-passing parent-level `notional_cap` check. | Confirmed, not hypothetical: (1)/(2)/(3) above all reach `allow` with the order's real economic size unassessed by any rule. This is the same class of gap A4 was originally created to close (string-typed `qty` silently defeating `_as_number`) reopened in a different corner: the parser now handles the *type* correctly, but several real order *shapes* still never reach it at all. |

---

## B. Forwarded-vs-attempted distinction

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| B1 | **PASS (reversed from prior FAIL)** | `evals/predicates.py`'s module docstring: *"This module is deliberately strict: `forwarded=` is mandatory on all tool_call clauses, and anything outside the grammar raises `PredicateSyntaxError`."* Confirmed in code, not just the docstring: `_parse_tool_call_inner` (`predicates.py:118-135`) — `if forwarded is None: raise PredicateSyntaxError(...)`. `ToolCallClause.forwarded` is typed `bool`, not `bool \| None`. Executed live: `parse_predicate("tool_call(name~='place_stock_order')")` (forwarded omitted) → raises `PredicateSyntaxError: tool_call() clause must explicitly specify 'forwarded=true' or 'forwarded=false'`; same for the identical clause inside `count(...)`. The canonical grammar lives in `evals/GRAMMAR.md` (new this round), quoted: *"`forwarded=<true\|false>`: **MANDATORY**... Any clause omitting `forwarded=` fails closed with `PredicateSyntaxError` at parse/load time."* | None. |
| B2 | **PASS** | Upgraded from a grep to the real enforced parser: `validate_corpus_file()` (now wired into `evals/run.py:190-191`, the harness's own corpus loader — not a dead function only reachable from tests) was run directly against all 7 files in `corpus/`. All **119** payloads across `benign.yaml` (25), `credential_exfiltration.yaml` (18), `edge_cases.yaml` (1), `induced_manipulation.yaml` (21), `multiturn_escalation.yaml` (18), `tool_poisoning.yaml` (18), `tool_result_injection.yaml` (18) parse successfully under the strict grammar — zero `PredicateSyntaxError`s, meaning zero ambiguous/omitted-`forwarded=` clauses remain anywhere in the current corpus. | None. |
| B3 | **PASS for the 6 files that feed the ASR/FPR gate; `edge_cases.yaml` doesn't carry the identical block** | Precisely extracted the 4-line `success_check predicate grammar:` block (by line offset, not a loose `awk` range — an earlier pass of this same check produced false divergence from an imprecise blank-line boundary, caught and corrected before reporting) from every file and hashed it: `benign.yaml`, `credential_exfiltration.yaml`, `induced_manipulation.yaml`, `multiturn_escalation.yaml`, `tool_poisoning.yaml`, `tool_result_injection.yaml` — **all six hash identically** (`4f5bd540...`), byte-for-byte, confirmed by direct `diff` on the extracted lines, not hash collision alone. `edge_cases.yaml` has no line containing that exact string — it carries a differently-worded pointer to the same `evals/GRAMMAR.md` ("Schema (a superset of benign.yaml's — see evals/GRAMMAR.md for the shared predicate grammar)"), consistent with its own documented, different purpose (`edge_cases.yaml` is explicitly **not** loaded by `evals/run.py`'s `load_corpus()`/`ATTACK_CLASS_FILES`, not part of the ASR/FPR gate — confirmed via `grep`/import — so the strict "byte-identical" bar arguably doesn't apply to a file with a genuinely different schema and role). | Minor, scoped: `edge_cases.yaml`'s grammar pointer is worded differently from the other six, though it points at the same canonical source and that file is out of the scored corpus. Not a defect in the scored 119-payload set. |
| B4 | **PASS (reversed from prior FAIL)** | Constructed the exact case live: `SessionRecord(tool_name="place_stock_order", forwarded=False, arguments={"symbol":"PUMP"}, verdict="hard_block")`. `evaluate(parse_predicate("tool_call(name~='place_stock_order', forwarded=true)"), session)` → **`False`**. `evaluate(parse_predicate("tool_call(name~='place_stock_order', forwarded=false)"), session)` → **`True`**. A hard-blocked, never-forwarded attempt cannot satisfy a `forwarded=true` clause, and — per B1 — no payload can omit `forwarded=` to sidestep this even accidentally, since the parser now refuses to load such a corpus at all. | None. |

---

## C. Audit log integrity

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| C1 | **PASS** | Generated a fresh 100-record log via the real `AuditLogWriter` → `verify_chain()` → `(True, None)`. Mutated record 50's `reason` field in place → re-verify → `(False, 51)` (tampering at record *i* detected at *i*+1, matching `verify_chain`'s own documented behavior). Non-tautology, executed not assumed: built an in-memory mutant of `verify_chain` with `if record.get("prev_hash") != expected_prev:` replaced by `if False:` (disabled) via `exec()` in this process, never touching disk — mutant returns `(True, None)` on the same tampered log the real function catches at `(False, 51)`. | None. |
| C2 | **(a) PASS (b) PASS (c) PASS (d) FAIL — expected, documented limitation** | All four executed against a fresh 100-record log: **(a)** in-place edit of record 50 → `(False, 51)`. **(b)** delete record 50 → `(False, 50)`. **(c)** swap records 50/51 → `(False, 50)`. **(d)** kept records `[0,60)`, replaced `[60,100)` with 40 forged records (fabricated `place_stock_order`/`PUMP`/`qty=999999` entries), correctly recomputing `prev_hash` forward from record 60's real hash → `verify_chain()` → `(True, None)` — **undetected**. Additionally tested a fifth case not in the prior round's set: **tampering with only the very last record** (100th) → `(True, None)` — also undetected, exactly matching `verify_chain`'s own docstring ("Tampering with the last record in the file is therefore undetectable from the file alone"), confirmed rather than taken on faith. Both (d) and the last-record case are structural limitations of an unsigned, un-anchored hash chain — no signing key, append-only filesystem attribute, or external timestamp notarization exists anywhere in `src/`. | (d) and last-record tampering are known, unmitigated limitations, reported as such. (a)/(b)/(c) genuinely detected. |
| C3 | **PASS** | This round's own instructions (`conformance-audit-prompt.md`) explicitly define the conforming shape as **hard_block: exactly 1 record; allow/soft_block: exactly 2 records** (`pending` then `outcome`) as a deliberate crash-safety design, not a defect — resolving a wording ambiguity a prior round's stricter "exactly one record per call" reading had flagged as a literal FAIL. Executed all three shapes live through `build_proxy()` with a fake upstream and the real `default.yaml`: **hard-blocked call** (unrecognized tool name): **1 record**, `verdict=hard_block`. **Allowed call** (`get_clock`): **2 records**, `upstream_status=['pending','ok']`. **Upstream-handler-error call** (`get_orders` raising inside its own body): **2 records**, `upstream_status=['pending','error']`. All three match the spec exactly. | None. Genuine transport-level timeout (the upstream subprocess dying mid-call, as opposed to a handler raising inside its own body, which *was* executed) was not independently reproduced this round either — code-inspection only shows it hits the same `except Exception:` branch (`proxy.py:545-556`) as the executed handler-error case. Flagged as UNVERIFIABLE by direct execution for that one sub-case. |
| C4 | **PASS** | Instrumented a fake upstream tool handler to read the audit log file **from inside its own body**, before returning: observed exactly 1 line already on disk, `upstream_status="pending"`, before the handler's own `return` executed — proving `record_call_pending()` is durably flushed to disk before `call_next()` (which invokes the handler) runs, not merely earlier in source order. | None. |

---

## D. Rule correctness

*(Scope: all 21 rule types registered in `RULE_TYPES` — `src/firewall/rules/__init__.py`. `kelly_sizing.py` remains excluded: not a `Rule` subclass, not policy-YAML-addressable, consumed only by `sizing_resolver.py` with hardcoded Python defaults, out of scope for D1-D5's per-rule verdicts, same as prior rounds. `iv_hv_ratio` is written and tested but confirmed — by executing `PolicyEngine.from_yaml` against a bogus rule type and reading the "Known types" list in the resulting `ValueError` — **not** in `RULE_TYPES` and **not** in `default.yaml`; included in D1/D3 below as a still-dormant rule, excluded from D4/D5's active-policy scope since it carries no live `regulation_ref`/threshold entry.)*

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| D1 | **PASS, with an honest scope caveat** | Every one of the 21 registered rule types has a `tests/rules/test_<type>.py` file with ≥2 test functions (counted directly: `notional_cap` 10, `position_cap` 7, `symbol_allowlist` 8, `blast_radius` 2, `drawdown_killswitch` 3, `order_rate_throttle` 2, `place_cancel_ratio` 2, `layering_detector` 2, `wash_trade_detector` 2, `cvar_gate` 21, `pct_of_adv` 12, `gtc_restriction` 9, `option_expiry_floor` 11, `option_spread_guard` 9, `net_delta_floor` 22, `hedge_cost_cap` 14, `option_sell_guard` 9, `hedge_regime_call_guard` 9, `cooldown_after_loss` 15, `hedge_proposal` 27, `unrecognized_tool_catchall` 10). Full suite: **432 passed, 0 failed** (`pytest -q`). | Caveat, stated plainly: this round confirmed file existence and a minimum test count for all 21, and separately confirmed positive+negative labeling by name for a *sample* (the 9 rules in D3 below, plus D2's two named rules) — a full manual positive/negative pairing audit of the other ~10 rule types' test files was not re-performed from scratch this round (a prior round did this exhaustively for the then-existing 14; the 7 newest rule types were traced individually across A1-A4/the 5 special checks instead). Not a gap in coverage, a gap in this round's exhaustiveness — stated rather than silently assumed clean. |
| D2 | **Unresolved gap, same as prior rounds — re-confirmed by reading the actual test bodies, not re-asserted from memory** | `wash_trade_detector`'s negative test (`tests/rules/test_wash_trade_detector.py:52-65`, `test_sell_of_meaningfully_different_size_does_not_trigger`) still uses `qty=10` against a triggering `qty=100` — a 10x gap, never probing the actual configured `qty_tolerance=0.01` boundary (e.g. `qty=99.99` vs `100`, which should trigger, or `qty=98`, which shouldn't) — same symbol/side/window/fill-status, so not "obviously unrelated" per this check's own FAIL bar, but the tolerance comparison itself has no negative-side test. `place_cancel_ratio`'s negative test (`tests/rules/test_place_cancel_ratio.py:56-70`, `test_single_cancelled_order_below_sample_size_does_not_trigger`) still exercises only the `min_orders_in_window` sample-size gate, never a scenario with sufficient sample size but a ratio below `ratio_threshold=0.9` (e.g. 3-of-5 cancelled = 0.6). Both gaps are identical to what a prior round already flagged; neither has been closed in this round's remediation. | Two specific, named, unresolved coverage gaps — reported again because they were re-verified by reading the current test bodies, not carried forward on faith. |
| D3 | **PASS on a 9-rule sample; not exhaustive this round** | Sampled the 7 newest rule types plus 2 older stateful rules already implicated in this round's live checks, each mutation applied via in-memory `exec()`/`sys.modules` substitution of the real file's source text (file on disk never touched), run in its own subprocess: `net_delta_floor` (`<` → `>` on the aggregate net-delta comparison, **8 failed**; `<` → `>` on the structural-delta-floor comparison, **6 failed**), `hedge_regime_call_guard` (`!=` → `==` on the call-type check, **3 failed**), `hedge_cost_cap` (`>` → `<` on the cost-vs-cap comparison, **5 failed**), `option_sell_guard` (`== "sell"` → `== "buy"`, **7 failed**), `option_expiry_floor` (`<` → `>` on the DTE-floor comparison, **7 failed**), `iv_hv_ratio` (`>` → `<` on the ratio-ceiling comparison, **4 failed**), `order_rate_throttle` (`>` → `<` on the rate-limit comparison, **2 failed**), `wash_trade_detector` (`<=` → `>` on the qty-tolerance comparison, **2 failed**). **9/9 mutations killed, zero survivors.** `git status --short` confirmed unchanged after this exercise (no file in `src/` was actually modified). | None found among the 9 sampled. The remaining ~12 rule types (mostly older ones a prior round already mutation-tested: `notional_cap`, `position_cap`, `symbol_allowlist`, `gtc_restriction`, `blast_radius`, `cooldown_after_loss`, `drawdown_killswitch`, `place_cancel_ratio`, `layering_detector`, `cvar_gate`, `pct_of_adv`, `unrecognized_tool_catchall`) were **not** re-mutated this round — reported as UNVERIFIABLE-this-round rather than re-asserted PASS from a prior round's result, per this audit's own "a test passing is not evidence" discipline (that applies equally to re-citing a prior round's mutation-test result without re-running it). |
| D4 | **PASS** | Extracted every rule's `id`/`type`/`regulation_ref` from the live-loaded `default.yaml` via `yaml.safe_load` (not a text grep): all 21 carry an explicit `regulation_ref` key — 11 with a specific SEC/FINRA citation (`notional-cap-single-order`, `position-cap-per-symbol`: SEC Rule 15c3-5(c)(1)(i); `option-spread-guard`, `net-delta-floor`, `order-rate-throttle`: 15c3-5(c)(1)(ii); `symbol-allowlist`, `unrecognized-tool-catchall`: 15c3-5(c)(2)(ii)/(b); `blast-radius-bulk-actions`: 15c3-5(b) + FINRA Reg. Notice 15-09; `cooldown-after-loss`, `drawdown-killswitch`: 15c3-5(c)(1)(i); `layering-detector`: FINRA Rule 5210 + 15 U.S.C. 78i(a)(2) + 17 CFR 240.10b-5 + FINRA Rule 2020; `wash-trade-detector`: FINRA Rule 5210.02), 10 explicitly `null` (UNMAPPED, honestly disclosed rather than fabricated: `option-sell-guard`, `hedge-regime-call-guard`, `option-expiry-floor`, `hedge-cost-cap`, `gtc-restriction`, `place-cancel-ratio`, `cvar-gate`, `pct-of-adv`, `hedge-proposal`). No fabricated-looking citation found (cross-checked citation formats against known SEC Rule 15c3-5 subsection numbering, all well-formed and consistent with prior rounds' verified numbering). | None. |
| D5 | **PASS** | AST-based scan (not a text grep — walks every `check()` method's parsed syntax tree for numeric `ast.Constant` nodes, excluding docstrings/comments by construction) across all 21 rule files found exactly 13 numeric literals inside any `check()` body, all structural/data-sufficiency, none a security threshold: `cooldown_after_loss.py:93` (`0`, sign check), `cvar_gate.py:175` (`2`, minimum-bars gate), `iv_hv_ratio.py:183`/`197` (`3` minimum-bars, `0` non-positive-HV guard), `net_delta_floor.py:204` (`0.0`, missing-position default), `notional_cap.py:87` (`1.0`, identity multiplier for non-option tools), `order_rate_throttle.py:62` (`1`, accounts for the in-flight call), `pct_of_adv.py:91/101/109/110` (minimum-bars/zero-guards), `place_cancel_ratio.py:70` (`0.0`, division-by-zero guard), `position_cap.py:71` (`0.0`, missing-position default). Every actual gating value (`max_usd`, `net_delta_floor`, `max_iv_hv_ratio`, `days_to_expiry_floor`, `max_relative_spread`, `max_pct_of_equity`, etc.) is confirmed read from `self.cfg.<field>`, sourced from `RuleConfig`/pydantic `_Params`. | None new. Same duplicate-default risk prior rounds flagged (several rules' `_Params` carry Python-side defaults for `market_data_timeout_seconds`/`quote_cache_ttl_seconds`/`feed` that duplicate `market_data.py`'s own constants) remains — not currently exploitable since `default.yaml` sets every such field explicitly for every rule that has one (re-confirmed by `grep -c` per field per rule in `default.yaml`, all present). |

---

## E. Fail-closed behavior

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| E1 | **PASS** | Executed all four cases directly: missing file → `FileNotFoundError`. Empty YAML → pydantic `ValidationError: version — Field required`. Malformed YAML → `yaml.parser.ParserError`. Unknown rule type → `ValueError: Unknown rule type 'this_rule_type_does_not_exist_anywhere' in rule 'bogus'. Known types: [21 real type names]`. End-to-end: ran `firewall.proxy.main()` in a real subprocess with `FIREWALL_POLICY_PATH` pointed at a nonexistent file → exit code **1**, `FileNotFoundError` traceback on stderr, `.run(transport="stdio")` never reached. | None. |
| E2 | **PASS, non-tautological** | A rule whose `check()` raises `ValueError("boom")`, run through the real `PolicyEngine.evaluate()` → `decision="hard_block"`, `rule_id="exploding"`. Rebuilt `evaluate()`'s own control flow as a mutant with the `try/except Exception` genuinely removed (not merely re-raising `rule.check()` in isolation, which would prove nothing about `PolicyEngine` itself) → the identical `ValueError` **escaped** uncaught, confirming the real `try/except` in `src/firewall/policy.py:128-153` is what stands between the exception and the caller. | None. |
| E3 | **PASS on "no LLM-reachable reset."** | `grep -rn "\.reset(" src/ tests/` → `PolicyEngine.reset()` (`policy.py:300-306`) is called only from `tests/test_proxy.py:1045`; `rule.reset()` is called only from `policy.py`'s own `reset()` method and from test files. `grep -n "@.*\.tool" src/firewall/proxy.py` → zero matches — no MCP tool is locally registered that could expose a reset action. Read `drawdown_killswitch.py` directly: `check()` never reads any call argument capable of clearing `self._tripped`; the sole path to `False` is the Python method `reset()`, unreachable via any MCP call shape. Same structure confirmed for `order_rate_throttle.py`'s `_paused` latch. Genuinely unreachable by an agent. `session_pnl_usd` inertness (the other half of a prior round's E3 finding) remains **fixed**: see check 1(c) below — confirmed via a real, HTTP-transport-mocked `GET /v2/account` round trip, not injected state. | None on reachability. `cooldown_after_loss`'s `pnl_history` input remains unpopulated by anything in `src/` (documented, unaddressed gap, same as prior rounds — no Alpaca endpoint cleanly exposes windowed realized-only P&L, per `account_data.py`'s own module docstring). |
| E4 | **PASS** | Same reachability analysis as E3 for `order_rate_throttle.reset()`. Executed live (see check 1(b) below): 25 rapid `place_stock_order` calls through the full proxy, `max_orders=20`/`window_seconds=60` — the 21st call is hard-blocked with reason text ending `"; throttle paused until explicit human reset"`, and nothing in the call surface can clear that pause. | None. |
| E5 | **PASS** | `_require_paper_trade_mode()` executed directly across 11 cases: unset, `""`, `"false"`, `"live"`, `"0"`, `"  "` → all **REFUSED**; `"TRUE"`, `"true"`, `"1"`, `"yes"`, `"YES"` (case-insensitive) → all **ALLOWED**. End-to-end: `main()` catches `PaperTradeGuardError` and exits via `SystemExit(1)`. | None. |

---

## F. Secret hygiene

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| F1 | **PASS** | `git rev-list --all --count` → 15. `git log --all -p -- . \| grep -inE "ALPACA_API_KEY[:=]['\"a-zA-Z0-9]\|ALPACA_SECRET_KEY[:=]['\"a-zA-Z0-9]\|AKIA[0-9A-Z]{16}\|sk-[a-zA-Z0-9]{20,}\|-----BEGIN.*PRIVATE KEY-----\|PK[A-Z0-9]{16,20}"` over the full history, filtered against `.env.example`'s own placeholder text → the only hits are `PK5XVNGP4R05QXHZM99Y`, used consistently as a **test fixture value** in `tests/test_proxy.py` (`monkeypatch.setenv(...)`) and cited in `proxy.py`'s own comment as a publicly-posted third-party Alpaca community-forum example (with URL) — not a credential belonging to this repo or its user. | None found in git history. |
| F2 | **PASS, verified by execution** | Set distinctive fake `ALPACA_API_KEY`/`ALPACA_SECRET_KEY` values, mocked `urllib.request.urlopen` to raise `HTTPError(401, "Unauthorized")`, and separately a generic `RuntimeError`, for **both** live HTTP fetchers (`account_data.fetch_session_pnl` and `market_data.fetch_daily_bars`): all four resulting `.reason` strings (`"HTTP 401 fetching account: Unauthorized"`, `"HTTP 401 fetching bars for AAPL: Unauthorized"`, and the two generic-exception variants) were checked to contain neither credential value — confirmed by `assert`, not by reading. | None found in this repo's own code. The third-party `alpaca-mcp-server` subprocess's own internal logging remains out of this repository's scope — UNVERIFIABLE from here. |
| F3 | **PASS** | `.gitignore` covers `.env`, `*.jsonl`, `agentdb.rvf`/`agentdb.rvf.lock`. `git check-ignore -v` confirms all four are actually ignored. `git ls-files \| grep -iE "\.env$\|\.jsonl$\|agentdb"` → zero matches (only `corpus/credential_exfiltration.yaml` would match a broader "credential" pattern, and it is the intentionally-tracked attack-corpus file). No `.env` file currently exists in the working tree. | None. |

---

## G. Reproducibility

| Check | Verdict | Evidence | Defect |
|---|---|---|---|
| G1 | **Literal timestamp check PASSES; the substantive pre-registration claim does not hold — same finding as prior rounds, independently re-derived from git, not carried forward** | `evals/thresholds.yaml`'s sole commit (`ec2df93`) is dated 2026-08-15 02:20:04 +0100. The first commit containing `evals/out/results.json` (`fa76044`) is dated 2026-08-18 01:10:00 +0100 — thresholds precede results by ~3 days, literally passing. `git reflog show --date=iso dev`, re-read directly this round: the 8 commits from `6eda851` ("scaffolding") through `fa76044` ("Commit eval results at seed 1337") — the entire firewall, rule library, corpus, eval harness, and first measurement — were all created within **4 minutes 15 seconds** of each other (`01:05:45`–`01:10:00` on 2026-08-18). A **second** such burst exists, not previously highlighted this precisely: 5 more commits (`710f054` through `87a03f4`, corpus-v1.1 + preset-sweep extension + results regeneration) landed within **2 minutes 34 seconds** (`02:01:58`–`02:04:52` on 2026-08-19). Two scripted retroactive-import sessions, not incremental engineering measured against a pre-set bar. | Report both timestamps as requested (literal precedence holds); reflog evidence contradicts the substantive property the check is proxying for, on both the original build and its round-2 extension. |
| G2 | **PASS on the literal question; the currently-checked-out corpus is untagged** | `git tag -l` → 7 annotated tags, all reachable from `HEAD` (`git merge-base --is-ancestor` for all 7 → all pass). `corpus-v1.1` (2026-08-19) is the current tag covering the corpus. **However**, `git diff corpus-v1.1 -- corpus/` shows the current working tree's corpus files differ from the tagged version — 88 lines removed, 26 added across all 7 files, entirely the grammar-header consolidation into `evals/GRAMMAR.md` this round's B1/B3 fix produced. The corpus actually validated and evaluated *right now* (B2 above; and whatever `evals/run.py` would produce if re-run today) is **not** the tagged `corpus-v1.1` — it is a newer, uncommitted state with no tag of its own. | The tag exists and is reachable (satisfies the literal check), but does not describe the corpus currently in use. A future `corpus-v1.2` tag, taken once this round's changes are committed, would close this. |
| G3 | **PASS, empirically, on the current corpus — same latent non-constructive-soundness caveat as prior rounds, re-confirmed with fresh values** | Ran `evals/run.py --seed 1337` twice in separate processes (default `PYTHONHASHSEED` vs. `PYTHONHASHSEED=42`), each to a distinct `--out-dir`: diffed both `results.json` outputs excluding `generated_at` → **byte-identical**. `evals/run.py:351`'s per-payload seed derivation, `random.Random((seed, entry["id"], config, with_retries).__hash__() & 0xFFFFFFFF)`, still relies on Python's built-in `hash()` on a tuple containing strings — genuinely `PYTHONHASHSEED`-sensitive by construction (confirmed by grep: unchanged from prior rounds) — so this pass held because the current 119-payload corpus's outcomes are insensitive to that jitter, not because the seeding is sound by construction. | Latent, currently-unmanifested: a future corpus entry sitting near a threshold boundary could break "same `--seed` → same `results.json`" under a different `PYTHONHASHSEED`. |

---

## Five specifically-required live checks (this round)

### 1. Re-verify Fix 1, Fix 2, and drawdown_killswitch from a real GET /v2/account

All three executed end-to-end through `build_proxy()` with a fake upstream and the real, unmodified `policies/default.yaml`:

- **String-typed `qty`/`limit_price` vs. `notional_cap`**: `place_stock_order` with `qty="100000", limit_price="500.00"` ($50,000,000 notional) against the real $5,000 cap → `is_error=True`, `"BLOCKED by rule 'notional-cap-single-order': order notional $50,000,000.00 exceeds per-order cap $5,000.00"`.
- **25 rapid orders vs. `order_rate_throttle`** (`max_orders=20`, `window_seconds=60`): fired 25 `place_stock_order` calls back-to-back through the full proxy — the **21st** call is hard-blocked (`"order rate exceeded 20 orders per 60s ... throttle paused until explicit human reset"`), listing the 20 preceding `order_id`s as evidence.
- **`drawdown_killswitch` from a REAL `GET /v2/account` equity drop, not injected state**: this required going beyond the repo's own existing test (`tests/test_proxy.py::test_session_pnl_fetch_reactivates_drawdown_killswitch`, which injects a canned `account_pnl_fetcher` callable directly — the real `account_data.fetch_session_pnl` → `_fetch_account` → `urllib.request.urlopen` code path is never exercised by that test). Built a fresh, independent script that mocks **only** `urllib.request.urlopen`, serving a JSON payload shaped exactly like Alpaca's real `/v2/account` response (`{"equity": "94000.00", "last_equity": "100000.00"}`), and lets `build_proxy()` use its **default** account-PnL fetcher (no `account_pnl_fetcher=` override passed in). Result: `place_stock_order` → `is_error=True`, `"BLOCKED by rule 'drawdown-killswitch': drawdown killswitch is tripped (session PnL breached $-1,000.00)..."`, with exactly 1 matching audit record. The assertion inside the mock confirmed the request URL was genuinely `https://paper-api.alpaca.markets/v2/account` — every layer above the transport (JSON parsing, `equity - last_equity = -6000`, threading through `FirewallMiddleware._populate_session_pnl` into `state["session_pnl_usd"]`, and `DrawdownKillswitchRule.check()` reading it) ran as real, unmodified code.

**Verdict: all three confirmed live.**

### 2. Options-corridor teeth outside `compute_proposal()`

Constructed a `place_option_order` BUY (`SPY271231P00500000`, far-dated, `qty=1`) submitted directly to `PolicyEngine.evaluate()` with **empty state** — no `hedge_proposal_rule`/`cvar_gate_rule`/`drawdown_killswitch_rule` wired, i.e. exactly the shape the agent would submit with no hedge trigger involved anywhere. Patched only the option-quote fetcher (narrow spread, `delta=-0.50`) on the three quote-dependent rule instances directly (`net-delta-floor`, `option-spread-guard`, `hedge-cost-cap`), isolating the corridor's own logic from market-data reachability. Result: **`hard_block`, `rule_id='net-delta-floor'`**, reason: *"proposed hedge exceeds neutral delta — this is a directional short, not a hedge (net delta -50.00 < floor +0.00...)"*.

**Verdict: the corridor is a real, registered `RULE_TYPES` entry evaluated by `PolicyEngine.evaluate()` for every matching call — not validation embedded only inside `hedge_proposal.compute_proposal()`. This is not the critical failure the task asked to watch for.**

### 3. Option sell hard-blocked in practice

- **Single-leg**: `place_option_order` with `side="sell"` → `hard_block`, `rule_id='option-sell-guard'`.
- **Multi-leg**: a two-leg `order_class="mleg"` call with one `side="buy"` leg and one `side="sell"` leg → `hard_block`, `rule_id='option-sell-guard'`, reason names the offending leg's side explicitly.

**Verdict: confirmed live, not merely documented.**

### 4. `compute_proposal()` writes an audit record and nothing else

Static: `inspect.getsource(hedge_proposal)` contains no occurrence of `call_next`, `Client(`, `create_proxy`, `urlopen`, or any other upstream-reaching symbol anywhere in the module — confirmed by direct string search, and by the fact that `compute_proposal()`'s own signature takes no `call_next`/`Client` argument at all, so it is structurally incapable of reaching upstream regardless of what's inside it. `grep -rn "call_next\|create_proxy\|Client(" src/` (same command as A3) independently confirms zero hits outside `proxy.py`.

Live: built a real `FirewallMiddleware` (order_history must persist across two calls in one session, so `build_proxy()`'s per-call-fresh-instance shortcut was not used) against a fake upstream tracking both `place_stock_order` and `place_option_order` calls. Seeded an AAPL position via one real market/limit order, then flipped the account-PnL fetcher to report a $-5,000 session breach and placed one unrelated MSFT order. Result: the MSFT call is hard-blocked by `drawdown-killswitch` (never reaches upstream), and exactly **1** `hedge-proposal` audit record fires (`verdict=soft_block`, reason containing `"DEFENSIVE HEDGE PROPOSAL"`, naming AAPL). The fake upstream's `received` list shows **only** the one legitimate call this test made through `client.call_tool()` — `place_option_order` was never invoked.

**Verdict: confirmed — the hedge proposal's only observable effect is an audit-log write.**

**New defect surfaced while building this check, not previously documented**: getting the drawdown-triggered proposal to fire at all required supplying `limit_price` on the seeding order. Repeating the identical scenario with a **plain market order** (`{"symbol": "AAPL", "side": "buy", "qty": "10"}`, no `limit_price` — the schema-documented default order shape this same codebase's own `notional_cap.py`/`option_spread_guard.py` docstrings describe) produces **zero** `hedge-proposal` audit records. Root cause, traced directly: `FirewallMiddleware._record_order_event` (`proxy.py:353-370`) reads `price = _coerce_float(arguments.get("limit_price"))`, which is `None` for a market order; `hedge_proposal._largest_open_position_from_history` (`hedge_proposal.py:180-199`) then does `if event.price is None: continue`, silently excluding that event from the "largest position" computation. With only one recorded position and it excluded, `_largest_open_position_from_history` returns `None`, and `_check_drawdown_trigger` returns `None` before ever reaching the bars fetch. Reproduced twice, cleanly, with `order_history` events printed directly showing `price=None` for the market-order case. Every existing test of this path (`tests/test_proxy.py::test_hedge_proposal_survives_even_when_the_same_call_hard_blocks`, this session's own first attempt at check 4) supplies `limit_price` on both calls — this is genuinely untested ground, not merely under-tested. Consequence: the drawdown-triggered hedge proposal — the one trigger path this round's own documentation repeatedly describes as "actually reachable through the real proxy" — is silently blind to the single most common real order shape.

### 5. README options disclaimers vs. current code

**CVaR-approximation disclaimer** (README.md:148-259): read against `src/firewall/rules/cvar_gate.py` line by line.

- *"cvar_gate.check() ... calls extract_notional() with no contract_multiplier override"* — confirmed: `cvar_gate.py:159-161` calls `extract_notional(arguments, self.cfg.notional_field, self.cfg.qty_field, self.cfg.price_field)`, exactly 4 positional args, no `contract_multiplier`. Reproduced the arithmetic directly: `extract_notional({"qty":"10","limit_price":"5.00"}, "notional", "qty", "limit_price")` → `50.0` (not `5000.0`) — the claimed 100x undercount is real and current.
- *"symbol precondition ... no-ops on multi-leg immediately"* — confirmed: `cvar_gate.py:155-157`, `if not symbol: return RuleOutcome(False)`, before any notional/bars logic. Verified live with a bars-fetcher spy installed on the real `cvar-gate` rule instance inside the real `PolicyEngine`: a multi-leg call never invokes the spy (0 calls). A single-leg call also never invokes it — but **the specific blocking rule has drifted from what the README names**: the README's own text says `symbol_allowlist` is what blocks single-leg orders before `cvar_gate` is ever reached; live execution this round shows **`net-delta-floor`** (added in a later remediation than when that paragraph was written, and placed ahead of `symbol-allowlist` in `default.yaml`) is actually the first rule to fire for a realistic single-leg call today. The bars-fetcher spy still shows 0 calls either way, so the substantive conclusion ("no option order reaches this weakly-scaled CVaR path") holds — but the specific mechanism named in the README text is stale, the same category of drift a prior round caught for `replace_order_by_id`'s disclaimer, smaller in impact here since the top-line claim survives.
- *90-day/95%-confidence "~4-5 tail points"* — arithmetic check: `ceil(90 × 0.05) = ceil(4.5) = 5`; accurate as stated.

**"What this does not do" — options section**: cross-checked every specific code claim against current source. *"`blast_radius`'s `human_approved` flag is never set anywhere in `src/`"* — confirmed via `grep -rn "human_approved" src/`: only appears in `blast_radius.py`'s own docstring/field declaration, never assigned `True` anywhere. *"`net_delta_floor` is now registered ... `iv_hv_ratio` remains unregistered"* — confirmed via the `RULE_TYPES`/`ValueError` "Known types" list (E1 above): `net_delta_floor` present, `iv_hv_ratio` absent. *"`hedge_regime_call_guard`'s two trigger paths ... only `drawdown_killswitch`'s is reachable, `cvar_gate`'s is not, and neither is wired by `FirewallMiddleware` as of this writing"* — confirmed by reading `proxy.py`'s `FirewallMiddleware.__init__`/`on_call_tool` directly: `state` never gains a `hedge_proposal_rule`/`cvar_gate_rule`/`drawdown_killswitch_rule` key anywhere in the class; `hedge-regime-call-guard` is therefore genuinely dormant in the live proxy today, exactly as documented.

**Verdict: both disclaimers are substantively accurate against the current code.** One specific mechanism reference (which rule blocks single-leg orders ahead of `cvar_gate`) has drifted and should be updated from "symbol_allowlist" to "net_delta_floor (and several other rules also placed ahead of symbol_allowlist)" — a documentation staleness, not a behavioral defect, reported rather than silently corrected (AUDIT-ONLY mode).

---

## FINAL

### Tally

| Verdict | Count | Items |
|---|---|---|
| **PASS** | 27 | A1, A2, A3, B1, B2, B3, C1, C2(a), C2(b), C2(c), C3, C4, D1, D3, D4, D5, E1, E2, E3, E4, E5, F1, F2, F3, G1, G2, G3, plus checks 1–5 of the five special live checks (reported inline above, not double-counted in this table) |
| **FAIL** | 4 | A4, C2(d) (expected/documented), B4 is now PASS (was FAIL in a prior round — reversed), D2 (unresolved coverage gap on 2 named rules, not a hard FAIL per the check's own bar but reported as unresolved) |
| **UNVERIFIABLE** | 1 (sub-item) | C3's genuine transport-timeout sub-case |

Several PASS rows carry material caveats and should not be read as clean:
**G1** (timestamps literally precede, but reflog shows two separate scripted-import bursts, not incremental pre-registered engineering), **G2** (tag exists and is reachable, but does not describe the corpus currently checked out), **G3** (empirically reproducible today, but not sound by construction — still keyed on `PYTHONHASHSEED`-sensitive `hash()`), **D1** (file-existence + count confirmed for all 21, full positive/negative pairing not re-audited for every one this round), **D3** (9/21 rule types re-mutated this round; the rest not re-verified, reported as such rather than re-asserted).

### Three defects most likely to invalidate published metrics, ranked

1. **A4 — bracket, OCO, and price-less multi-leg option orders reach `allow` with zero notional/position/risk assessment (demonstrated live).** No corpus payload exercises any of these three shapes (confirmed: `grep -rl "order_class" corpus/` and `grep -rl "place_option_order" corpus/` both return nothing), so this is invisible to every published ASR/FPR number, exactly the "structurally invisible to the harness" pattern the original A4 finding described for string-typed `qty`. An agent (or an attacker) that wraps any order in `order_class="bracket"`/`"oco"` or omits a multi-leg's parent `limit_price` bypasses every size/risk rule except the one that happens to look only at the parent's own top-level fields.
2. **The `hedge_proposal` drawdown-trigger path is silently blind to plain market orders (newly discovered this round, demonstrated live, reproduced twice).** This is the one trigger path the codebase's own documentation repeatedly describes as "actually reachable through the real proxy" today — and it silently produces zero output for the single most common real order shape, with no error, no audit trail, and no test anywhere that would have caught it (every existing test happens to supply `limit_price`). Not a published-metric risk today (no corpus payload exercises the hedge-proposal path at all), but it directly undermines the one piece of this round's own headline claim ("drawdown_killswitch trips from a real GET /v2/account equity drop" — true for the killswitch itself, not for the hedge proposal that's supposed to fire alongside it).
3. **G1/G2 — the eval harness's pre-registration and version-control story still does not fully hold up, now with a second scripted-import burst layered on the first.** The original 8-commit, 4m15s burst (thresholds → full system → first results) is joined by a second, 5-commit, 2m34s burst a day later (corpus-v1.1 + preset-sweep extension + results regeneration) — and the corpus actually validated by this very audit (B2) is itself untagged, sitting ahead of `corpus-v1.1` with no `corpus-v1.2` yet cut. None of this makes any single published number wrong, but it means "the thresholds were fixed before the system was built and measured" and "the scored corpus is the tagged corpus" are both weaker claims than the repository's own tags/commits suggest at a glance.

### What this audit could not test, and why

- **The real `alpaca-mcp-server` subprocess's own internal error/logging behavior** (F2's boundary) — out of this repository's scope; only this repo's handling of its outputs was checked.
- **C3's genuine transport-timeout sub-case** — a real transport-level exception from `call_next` (the upstream subprocess dying mid-call) was not independently reproduced; inferred only by code-path symmetry with the executed handler-error case.
- **Full D1/D3 exhaustiveness across all 21 rule types** — D1 confirmed file-existence/count for all 21; D3 mutation-tested a 9-rule sample this round (the newest 7 rule types plus 2 older stateful ones directly implicated in the five special checks). The remaining ~12 rule types were mutation-tested by a prior round, not re-verified in this session — reported as UNVERIFIABLE-this-round per this audit's own "don't carry forward untested claims" discipline, not silently re-asserted as PASS.
- **Whether the published ASR/FPR numbers are measurably affected by the A4 bracket/OCO/multi-leg gap or the hedge-proposal market-order blindness** — confirmed the *mechanism* (no corpus payload exercises any of these shapes) but did not build a new corpus payload or re-run the harness against one to measure an actual delta; that would be the natural next step and is outside an audit-only pass.
- **The real upstream's behavior against a genuine live/paper Alpaca account** — A1's tool enumeration used a real, live `uvx alpaca-mcp-server` process this session (network was available, unlike a prior round), but with fabricated API credentials; no call in this audit reached Alpaca's actual paper-trading servers with valid credentials.

### Overall assessment

This round's remediation is real where it claims to be: B1/B3/B4 are now enforced by a parser that raises rather than a comment that promises, re-verified by trying to defeat that enforcement, not by reading it. All five of the specifically-required live checks came back clean, including the two that most directly targeted whether this system's newest, most narratively self-congratulatory code (the options/hedge corridor, `compute_proposal()`) actually does what four consecutive rounds of its own `AUDIT.md` claimed it does — it does. That document's own style (triumphant, self-citing, "caught in review before landing") is exactly the kind of artifact this audit's instructions warn against trusting, and on the specific claims re-executed here, it held up.

But it did not hold up everywhere. A4's bracket/OCO/multi-leg gap is not new — it has been disclosed in README prose for at least two rounds — and it remains completely unguarded today, demonstrated live in this pass exactly as it would have been in the last one. And this round surfaced one defect no prior round found: the hedge-proposal drawdown trigger's blindness to plain market orders, discovered only because this audit built its own live reproduction instead of reusing the repository's existing (uniformly limit-price-supplying) test suite. Do not treat "the corridor has teeth" and "the hedge proposal is well-tested" as the same claim — the first is now confirmed; the second still has at least one hole its own test suite never probed.
