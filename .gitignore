# Environment
.env
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Logs / audit output
*.jsonl
data/runtime_heartbeat.json
data/current_cycle.json
data/order_lifecycle.json
data/last_market_brief.json
data/sec_edgar_current_ratios_cache.json
data/usaspending_contract_links_cache.json
data/cycles/*.json

# Editors
.vscode/
.idea/

# Unrelated local tooling artifact (binary agent-memory lock file, not
# produced by or read by anything in this repo)
agentdb.rvf
agentdb.rvf.lock

# Local agent worktrees are separate repositories, never project content.
.claude/worktrees/
.checkpoint_verify/
